using System.Collections.Generic;
using Utils;
using Xunit;

namespace Test
{
    public class FilterExtensionTest
    {/*
        [Fact]
        public void FilersEvenNumbers()
        {
            var input = new List<int> {1, 2, 3, 4, 5};
            var output = new List<int> {2, 4};

            Assert.Equal(output, input.Even());
        }

        [Fact]
        public void FilersOddNumbers()
        {
            var input = new List<int> {1, 2, 3, 4, 5};
            var output = new List<int> {1, 3, 5};

            Assert.Equal(output, input.Odd());
        }

        [Fact]
        public void FilersOddAndEvenNumbers()
        {
            var input = new List<int> {1, 2, 3, 4, 5};

            Assert.Empty(input.Odd().Even());
            Assert.Empty(input.Even().Odd());
        }

        [Fact]
        public void CanTakeOnlySomeNumbers()
        {
            var input = new List<int> {1, 2, 3, 4, 5};
            var output = new List<int> {1, 2, 3};

            Assert.Equal(output, input.Only(3));
        }

        [Fact]
        public void HandlesGracefullyNullValues()
        {
            List<int>? input = null;

            Assert.Empty(input.Even());
            Assert.Empty(input.Odd());
            Assert.Empty(input.Only(1));
        }*/
    }
}